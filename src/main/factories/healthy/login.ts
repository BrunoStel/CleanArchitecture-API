
// eslint-disable-next-line @typescript-eslint/explicit-function-return-type
export const makeHealthyController = () => {
  const statusCode = 200
  const body = 'OK'
  return { statusCode, body }
}
